# INSERT

Для добавления новых данных в таблицу используется команда INSERT:
```SQL
CREATE DATABASE Productsdb;
USE productsdb;
CREATE TABLE Products(
	Id INT AUTO_INCREMENT PRIMARY KEY,
	ProductName VARCHAR(30) NOT NULL,
	Manufacturer VARCHAR(20) NOT NULL,
	ProductCount INT DEFAULT 0,
	Price DECIMAL NOT NULL
);

INSERT Products(ProductName, Manufacturer, ProductCount, Price)
VALUES 
('IPhone X', 'Apple', 5, 76000),
('Galaxy S8', 'Samsung', 2, 46000);
```

# SELECT

Для выборки данных из базы данных используется команда SELECT:

```SQL
CREATE DATABASE Productsdb;
USE productsdb;
CREATE TABLE Products(
	Id INT AUTO_INCREMENT PRIMARY KEY,
	ProductName VARCHAR(30) NOT NULL,
	Manufacturer VARCHAR(20) NOT NULL,
	ProductCount INT DEFAULT 0,
	Price DECIMAL NOT NULL
);
```

Для получения всех данных:

```SQL
SELECT * FROM Products;
```

Если необходимо получить конкретные столбцы:

```SQL
SELECT ProductName, Price FROM Products;
```

Спецификация столбца необязательно должна представлять его название. Это может быть любое выражение, например, результат арифметической операции. Так, выполним следующий запрос:

```SQL
SELECT ProductName, Price * ProductCount
FROM Products;
```

С помощью оператора AS можно изменить название выходного столбца или определить его псевдоним:

```SQL
SELECT ProductName, Price * ProductCount AS TotalSum
FROM Products;
```

# WHERE

Зачастую необходимо извлекать не все данные из БД, а только те, которые соответствуют определенному условию. Для фильтрации данных в команде SELECT применяется оператор WHERE, после которого указывается условие

Если условие истинно, то строка попадает в результирующую выборку. В качестве можно использовать операции сравнения, которые сравнивают два выражения:

- =: сравнение на равенство
    
- !=: сравнение на неравенство
    
- <>: сравнение на неравенство
    
- <: меньше чем
    
- >: больше чем
    
- <=: меньше чем или равно
    
- >=: больше чем или равно

Например:

```SQL
SELECT * FROM Products
WHERE Manufacturer = 'Samsung';
```

Для MySQL регистр символов не важен, поэтому Samsung Будет эквивалентен SAMSUNG
Также при поиске можно использовать выражения:

```SQL
SELECT * FROM Products
WHERE Price * ProductCount >100000;
```

Также для WHERE применимы логические операторы AND, OR, NOT:

```SQL
SELECT * FROM Products
WHERE Manufacturer = 'Samsung' AND Price > 50000;
```

При нескольких логических операциях первой по приоритету идет NOT, потом AND, потом OR. Приоритет операций можно переопределить круглыми скобками:

```SQL
SELECT * FROM Products
WHERE Manufacturer = 'Samsung' OR NOT (Price > 30000 AND ProductCount > 2);
```


# UPDATE
Команда UPDATE используется для обновления уже имеющихся строк. Например, увеличим у всех имеющихся товаров цену на 3000:

```SQL
UPDATE Products
SET Price = Price + 3000;
```

Теперь поменяем Manufacturer Samsung на Samsung Inc.

```SQL 
UPDATE Proudcts
SET Manufacturer = 'Samsung Inc.'
WHERE Manufacturer = 'Samsung';
```

Также можно обновлять сразу несколько столбцов:

```SQL
UPDATE Products
SET Manufacturer = 'Samsung Inc.'
	ProductCount = ProductCount + 3
WHERE Manufacturer = 'Samsung';
```

# DELETE

Для удаления строк используется команда DELETE:

```SQL
DELETE FROM Products
WHERE Manufacturer = 'Samsung Inc.';
```

Также мы можем удалить все данные:

```SQL
DELETE FROM Products;
```

# Источники

Метаинт https://metanit.com/sql/mysql/3.1.php